{
  "name": "api-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers API for Site360",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .wrangler node_modules"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.22.0"
  }
}
